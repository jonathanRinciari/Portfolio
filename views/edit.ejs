

<% include ./partials/header %>

      <form action="/blogs/<%=blog._id%>?_method=PUT" method="POST">
        <div >
          <input type="text" value="<%= blog.name %>" name="blog[name]">
        </div>
        <div>
          <input type="text" value="<%= blog.image %>" name="blog[image]">
        </div>
        <div>
          <input class="form-control" type="text" value="<%= blog.description %>" name="blog[description]">
        </div>
        <div>
          <button class="btn-lg btn-primary btn-block"> Submit! </button>
        </div>
      </form>
      <a href="/blogs"> View All Blogs</a>
    </div>
    
  </div>
</div>
<% include ./partials/footer %>